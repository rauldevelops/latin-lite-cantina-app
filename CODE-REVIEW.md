# Carlo's Crazy Code-Review

## `schema.prisma`
- You want to create a `Customer` model that has a `User` relation inside of it to define your customer-specific fields
- Same thing with `Driver` and `Kitchen`